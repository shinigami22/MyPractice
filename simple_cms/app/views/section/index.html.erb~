<% if !flash[:notice].blank? %>
<div class="notice">
        <%= flash[:notice]%>
</div>
<% end %>



<div class="section index">
<h2>Pages</h2>

<%= link_to("Add New Section", {:action => 'new'}, :class => 'action new')%>

<table class="listing" summary="Page list">
        <tr class="header">
        <th>&nbsp;</th>
        <th>Section</th>
        <th>Visible</th>
        <th>Content</th>
        <th>Actions</th>
        </tr>
	<% @section.each do |sec| %>
        <tr>
        <td><%= sec.position %></td>
        <td><%= sec.name %></td>
        <td class="center"><%= sec.visible ? 'Yes' : 'No' %></td>
        <td class="center"><%= sec.content %></td>
        <td class="actions">
        <%= link_to("Show", {:action => 'show', :id => sec.id}, :class => 'action show')%>
        <%= link_to("Edit", {:action => 'edit', :id => sec.id}, :class => 'action edit')%>
        <%= link_to("Delete",{:action => 'delete', :id => sec.id}, :class => 'action delete')%>
        </td>
        </tr>
        <% end %>
        </table>
</div>

                                   
